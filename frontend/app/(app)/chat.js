import React, { useState, useEffect, useContext, useRef } from 'react';\nimport { StyleSheet, View, Text, FlatList, TextInput, TouchableOpacity, ActivityIndicator, KeyboardAvoidingView, Platform } from 'react-native';\nimport { LinearGradient } from 'expo-linear-gradient';\nimport { AuthContext } from '../../context/AuthContext';\nimport io from 'socket.io-client';\n\nconst Chat = ({ route }) => {\n  const { userId, username } = route.params || {};\n  const [messages, setMessages] = useState([]);\n  const [inputText, setInputText] = useState('');\n  const [loading, setLoading] = useState(false);\n  const { user, token } = useContext(AuthContext);\n  const socketRef = useRef(null);\n  const flatListRef = useRef(null);\n\n  useEffect(() => {\n    if (userId) {\n      fetchMessages();\n      connectSocket();\n    }\n\n    return () => {\n      if (socketRef.current) {\n        socketRef.current.disconnect();\n      }\n    };\n  }, [userId]);\n\n  const fetchMessages = async () => {\n    setLoading(true);\n    try {\n      const response = await fetch(\n        `${process.env.EXPO_PUBLIC_API_URL}/api/messages/conversation/${userId}`,\n        { headers: { 'Authorization': `Bearer ${token}` } }\n      );\n      const data = await response.json();\n      setMessages(Array.isArray(data) ? data : []);\n    } catch (err) {\n      console.error('Fetch messages error:', err);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const connectSocket = () => {\n    socketRef.current = io(process.env.EXPO_PUBLIC_API_URL);\n\n    socketRef.current.on('connect', () => {\n      socketRef.current.emit('join_room', `chat_${user.id}_${userId}`);\n    });\n\n    socketRef.current.on('receive_message', (data) => {\n      setMessages((prev) => [...prev, data]);\n    });\n  };\n\n  const sendMessage = async () => {\n    if (!inputText.trim()) return;\n\n    try {\n      const response = await fetch(`${process.env.EXPO_PUBLIC_API_URL}/api/messages/send`, {\n        method: 'POST',\n        headers: {\n          'Content-Type': 'application/json',\n          'Authorization': `Bearer ${token}`\n        },\n        body: JSON.stringify({\n          receiver_id: userId,\n          content: inputText,\n          is_anonymous: false\n        })\n      });\n\n      const newMessage = await response.json();\n\n      if (response.ok) {\n        setMessages((prev) => [...prev, newMessage.data]);\n        setInputText('');\n\n        // Emit via socket for real-time\n        if (socketRef.current) {\n          socketRef.current.emit('send_message', {\n            room: `chat_${user.id}_${userId}`,\n            text: inputText,\n            sender: user.username\n          });\n        }\n      }\n    } catch (err) {\n      console.error('Send message error:', err);\n    }\n  };\n\n  const MessageBubble = ({ message, isOwn }) => (\n    <View style={[styles.messageBubbleContainer, isOwn && styles.ownMessage]}>\n      <View style={[styles.messageBubble, isOwn ? styles.ownBubble : styles.otherBubble]}>\n        <Text style={[styles.messageText, isOwn && styles.ownMessageText]}>\n          {message.content}\n        </Text>\n      </View>\n    </View>\n  );\n\n  return (\n    <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={styles.container}>\n      {/* Header */}\n      <LinearGradient colors={['#667eea', '#764ba2']} style={styles.header}>\n        <Text style={styles.headerTitle}>💬 {username || 'Chat'}</Text>\n      </LinearGradient>\n\n      {/* Messages */}\n      {loading ? (\n        <View style={styles.centerContainer}>\n          <ActivityIndicator size=\"large\" color=\"#667eea\" />\n        </View>\n      ) : (\n        <FlatList\n          ref={flatListRef}\n          data={messages}\n          keyExtractor={(item) => item.id?.toString()}\n          renderItem={({ item }) => (\n            <MessageBubble message={item} isOwn={item.sender_id === user.id} />\n          )}\n          contentContainerStyle={styles.messagesContainer}\n          onContentSizeChange={() => flatListRef.current?.scrollToEnd({ animated: true })}\n        />\n      )}\n\n      {/* Input */}\n      <View style={styles.inputContainer}>\n        <TextInput\n          style={styles.input}\n          placeholder=\"Type a message...\"\n          placeholderTextColor=\"#999\"\n          value={inputText}\n          onChangeText={setInputText}\n          multiline\n          maxHeight={100}\n        />\n        <TouchableOpacity style={styles.sendButton} onPress={sendMessage}>\n          <Text style={styles.sendButtonText}>Send</Text>\n        </TouchableOpacity>\n      </View>\n    </KeyboardAvoidingView>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: '#f8f9fa'\n  },\n  header: {\n    paddingHorizontal: 20,\n    paddingVertical: 20,\n    paddingTop: 40\n  },\n  headerTitle: {\n    fontSize: 22,\n    fontWeight: 'bold',\n    color: '#fff'\n  },\n  centerContainer: {\n    flex: 1,\n    justifyContent: 'center',\n    alignItems: 'center'\n  },\n  messagesContainer: {\n    flexGrow: 1,\n    padding: 15,\n    justifyContent: 'flex-end'\n  },\n  messageBubbleContainer: {\n    marginBottom: 10,\n    flexDirection: 'row'\n  },\n  ownMessage: {\n    justifyContent: 'flex-end'\n  },\n  messageBubble: {\n    maxWidth: '80%',\n    paddingVertical: 10,\n    paddingHorizontal: 15,\n    borderRadius: 20\n  },\n  ownBubble: {\n    backgroundColor: '#667eea',\n    borderBottomRightRadius: 5\n  },\n  otherBubble: {\n    backgroundColor: '#e0e0e0',\n    borderBottomLeftRadius: 5\n  },\n  messageText: {\n    fontSize: 14,\n    color: '#333'\n  },\n  ownMessageText: {\n    color: '#fff'\n  },\n  inputContainer: {\n    flexDirection: 'row',\n    paddingHorizontal: 15,\n    paddingVertical: 10,\n    paddingBottom: 20,\n    backgroundColor: '#fff',\n    borderTopWidth: 1,\n    borderTopColor: '#eee'\n  },\n  input: {\n    flex: 1,\n    backgroundColor: '#f0f0f0',\n    borderRadius: 20,\n    paddingHorizontal: 15,\n    paddingVertical: 10,\n    marginRight: 10,\n    maxHeight: 100,\n    color: '#333'\n  },\n  sendButton: {\n    backgroundColor: '#FF6B6B',\n    borderRadius: 20,\n    width: 50,\n    height: 50,\n    justifyContent: 'center',\n    alignItems: 'center'\n  },\n  sendButtonText: {\n    color: '#fff',\n    fontWeight: '600',\n    fontSize: 14\n  }\n});\n\nexport default Chat;\n